{
  "name": "syncwatch-types",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npx -y tsc --build",
    "compile": "tsc --noEmit"
  }
}
