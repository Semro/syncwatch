{
  "name": "syncwatch",
  "version": "1.0.0",
  "description": "syncwatch-root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/syncwatch-server",
    "packages/syncwatch-locales",
    "packages/syncwatch-extension",
    "packages/syncwatch-types"
  ],
  "scripts": {
    "lint": "npx @biomejs/biome check format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.1"
  },
  "dependencies": {
    "typescript": "^5.6.2"
  }
}
